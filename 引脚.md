3.控制接口说明
控制接口包括两部分，其中主接口完成模块绝大部分功能，包括供电、通信、状态反馈、
中断等。信号均为3.3V电平。
主接口，SPIPDI/中断/电源接口，提供完整的控制端口
通信辅助信号接口，提供网络通信指示，可以不使用
SPIPDI/中断/电源接口信号定义
管脚号信号名功能描述模块管脚类型
1. SPI_CS SPI片选I
2. SPI_CLK SPI时钟I
3. GND数字地GND
4. GND数字地GND
5. SPI_MOSI SPI主输出从输入I
6. SPI_MISO SPI主输入从输出O
7. GND数字地P
8. Q_RESET复位控制，I/O
9. SPI_IRQ IRQ中断I
10. EEPROMLOAD EEPROMLOAD完成O
11. SYNC0 SYNC0同步信号O
12. SYNC1 SYNC1同步信号O
13. GND数字地P
14. GND数字地P
15. VCC5V 5V供电输入P
16. VCC5V 5V供电输入P
通信辅助信号接口信号定义
管脚号信号名功能描述模块管脚类型
1. PORT0LINKACT输入端口通信状态O
2. RUNLED AL状态O
3. PORT1LINKACT输出端口通信状态O
4. GND数字地P
4.管脚功能说明
4.1. SPI接口
MCU与模块的主要通信接口，配合XML支持多种SPI模式。最高通信速率20M。该端
口具有以下特点：
可配置spi片选为高或低有效
可配置无访问时spi时钟电平
可配置数据采样时钟沿
4.2. 复位接口
此管脚为IO管脚，表征模块当前状态或对模块进行复位控制。可连接MCU 开漏模式
GPIO 进行复位控制。MCUGPIO拉低时该信号可对模块进行复位。模块会在以下情况下输出
低。
 上电复位
 软件写0x0040进行复位
4.3. EEPROMLOAD
模块复位完成。正确加载完EEPROM内的XML信息后，EEPROMLOAD会输出高电平，
表征模块准备完成。
4.4. 端口LED 说明
网口左侧黄色LED灯表明网络链接状态和数据传输状态。行为如下：
 熄灭，没有网络链接
 常亮，网络稳定链接，没有数据传输
 闪烁，网络稳定链接，有数据传输